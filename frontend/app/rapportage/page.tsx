'use client'
import {useEffect,useState} from 'react';import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';import {getReporting} from '../../components/api';
export default function P(){const [d,setD]=useState<any>();useEffect(()=>{getReporting().then(setD)},[]);if(!d)return <p>laden...</p>; const k=d.kpis; return <div><h2>Rapportage</h2><div style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:8}}>{Object.entries(k).map(([key,v])=><div key={key} style={{border:'1px solid #ddd',padding:8}}><b>{key}</b><div>{Number(v as number).toFixed(2)}</div></div>)}</div><div style={{height:300}}><ResponsiveContainer><BarChart data={d.positions}><XAxis dataKey='asset_name'/><YAxis/><Tooltip/><Bar dataKey='realized_pnl_eur' fill='#2563eb'/></BarChart></ResponsiveContainer></div></div>}
